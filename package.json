{
  "name": "saluvia-electrosurgical",
  "private": true,
  "description": "Monorepo root. The Next.js app lives in /web — set Vercel Root Directory to web.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  }
}
